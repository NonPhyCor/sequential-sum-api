# Sequential Sum API

A secure RESTful API built with Python and FastAPI that calculates the sequential sum of a list of numbers.

## Setup Instructions

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/your-username/sequential-sum-api.git](https://github.com/your-username/sequential-sum-api.git)
   cd sequential-sum-api